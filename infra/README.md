# Infraestructura

## Responsable: Escobar

## Estructura
- `docker-compose.yml` — orquestación de servicios (placeholder, ver comentarios TODO)
- `mongo/` — scripts de inicialización del Replica Set MongoDB
- `postgres/` — scripts DDL de inicialización de PostgreSQL

## Pendiente
- Configurar 3 nodos MongoDB Replica Set.
- Configurar PostgreSQL streaming replication.
- Activar servicios en docker-compose.yml conforme avance la implementación.
